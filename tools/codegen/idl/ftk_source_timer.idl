#include "ftk_typedef.idl"

interface FtkSourceTimer
{
	FtkSourcePtr ftk_source_timer_create(in int interval, in FtkTimerFunc action, in voidPtr user_data);
	Ret ftk_source_timer_reset(in FtkSourcePtr thiz);
};
